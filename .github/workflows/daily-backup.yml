name: Daily ESHOT Backup

on:
  schedule:
    # TSİ 23:00 = UTC 20:00
    - cron: "0 20 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run backup script
        run: npm run backup

      - name: Commit and push if changed
        run: |
          git diff --quiet -- data/eshot.db && \
          git ls-files --others --exclude-standard -- data/eshot.db | grep -q . || {
            echo "Degisiklik yok, commit atlanacak."
            exit 0
          }

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/eshot.db
          git commit -m "chore: daily eshot backup $(date -u +'%Y-%m-%d')"
          git push
